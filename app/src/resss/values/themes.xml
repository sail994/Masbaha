cat << 'EOF' > ~/masbaha/app/src/main/res/values/themes.xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de base utilisant Material 3 pour éviter les conflits avec Compose -->
    <style name="Theme.Masbaha" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Couleur de la barre de statut (en haut) -->
        <item name="android:statusBarColor">?attr/colorSecondaryContainer</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
EOF
